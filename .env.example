# Database URL
# For local development (SQLite):
DATABASE_URL=sqlite:///./bounties.db

# For production (PostgreSQL on Railway):
# DATABASE_URL=postgresql://user:password@host:5432/bounties

# Server settings
HOST=0.0.0.0
PORT=8000

# Optional: Path to local ACP skill for direct registry scanning
# ACP_SKILL_PATH=/path/to/virtuals-protocol-acp
